#include "utils.h"

/**
 * check the parameter *host valid or not.
 *
 * Maybe, it is no need to do so
 */
unsigned int check_host(const char* host)
{
  return 1;
}

/**
 * see check_host
 */
unsigned int check_ip(const char* ip)
{
	return 1;
}

